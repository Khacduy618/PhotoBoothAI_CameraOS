# Capture Rules

Before capture:

- camera ready
- video dimensions valid
- state allows capture
- no capture already running

Preserve original capture.

Guard duplicate capture requests.

Associate captures with stable session and capture IDs.
