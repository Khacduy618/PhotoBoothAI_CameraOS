# Next.js Rules

Preserve Server/Client boundaries.

Use client components only for browser/device APIs or local interactivity.

Avoid heavy work in render.

Keep device lifecycle cleanup explicit.

Validate server inputs.

Do not expose local filesystem paths publicly.
