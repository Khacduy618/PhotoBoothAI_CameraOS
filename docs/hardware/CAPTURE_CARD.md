# Capture Card

A capture card exposes a video input to the computer.

It does not automatically provide camera control or original high-resolution still capture.

Document:

- vendor/model
- USB mode
- supported resolutions
- frame rate
- latency
- color behavior
- disconnect behavior
- device ID stability
- hub compatibility
